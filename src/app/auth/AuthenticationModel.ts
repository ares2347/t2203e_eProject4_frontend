export interface LoginFormModel {
    identifier?: string;
    password?: string;
}

export interface SignupFormModel {
    fullName?: string;
    email?: string;
    phone?: string;
    password?: string;
}